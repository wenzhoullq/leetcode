## 模板
 * DP
```java
for(int i=s.length()-1;i>=0;i--){
    char c1=s.charAt(i);
    for(int j=i;j<s.length();j++){
        char c2=s.charAt(j);
        if(i==j) dp[i][j]=1;
        if(c1==c2)) dp[i][j]=dp[i+1][j-1]+2;
        else dp[i][j]=Math.max(dp[i+1][j],dp[i][j-1]);
    }
}
```
* 贪心
```java
  for(int i=0;i<s.length();i++){
    char c1=s.charAt(i), c2=s.charAt(s.length()-i-1);
    if(c1!=c2){
        递归(s.substring(i+1,s.length()-i-1))||递归(s.substring(i,s.length()-i-2));
    }
}
```
* 中心扩散
```java
    for(int i=0;i<s.length();i++){
            int len1=isrever(i,i),len2=isrever(i,i+1);
            int len=Math.max(len1,len2);
            if(len>end-start+1){
                start=i-(len-1)/2;
                end=i+len/2;
            } 
        }
```
5. 最长回文子串.md

## 题目
给你一个字符串 s，找到 s 中最长的回文子串。
* 中心扩散
```java
  class Solution {
    String s;
    public String longestPalindrome(String s) {
        this.s=s;
        int start=0,end=0;
        for(int i=0;i<s.length();i++){
            int len1=isrever(i,i),len2=isrever(i,i+1);
            int len=Math.max(len1,len2);
            if(len>end-start+1){
                start=i-(len-1)/2;
                end=i+len/2;
            }
           
        }
        return s.substring(start,end+1);
    }
    int isrever(int left,int right){
        while(left>=0&&right<s.length()&&s.charAt(left)==s.charAt(right)){
            left--;
            right++;
        };
        return right-left-1;
    }
}
```
* DP

```java
class Solution {
    public String longestPalindrome(String s) {
        String ans=null;
        int length=s.length(),len=0;
        boolean[][] dp=new boolean[length][length];
        for(int i=0;i<length;i++){
            for(int j=0;j<=i;j++){
                if(s.charAt(i)==s.charAt(j)&&(i-j<=2||dp[j+1][i-1])){
                    dp[j][i]=true;
                    if(i-j+1>len) {
                        len=i-j+1;
                        ans=s.substring(j,j+len);
                    }
                }
            }
        }
        return ans;
    }
}
```
1312. 让字符串成为回文串的最少插入次数

## 题目
给你一个字符串 s ，每一次操作你都可以在字符串的任意位置插入任意字符。

请你返回让 s 成为回文串的 最少操作次数 。

「回文串」是正读和反读都相同的字符串。

## 思路
length-最大回文
```java
class Solution {
    public int minInsertions(String s) {
        int length=s.length();
        int[][] dp=new int[length][length];
        for(int i=length-1;i>=0;i--){
            char c1=s.charAt(i);
            for(int j=i;j<length;j++){
                if(i==j){
                    dp[i][j]=1;
                    continue;
                } 
                char c2=s.charAt(j);
                if(c1==c2) dp[i][j]=dp[i+1][j-1]+2;
                else dp[i][j]=Math.max(dp[i+1][j],dp[i][j-1]);
            }
        }
        return length-dp[0][length-1];
    }
}
```
